<%= form_for(@consequence) do |f| %>
  <% if @consequence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consequence.errors.count, "error") %> prohibited this consequence from being saved:</h2>

      <ul>
      <% @consequence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :topic_id %><br>
    <%= f.text_field :topic_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
