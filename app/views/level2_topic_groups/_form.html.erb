<%= form_for(@level2_topic_group) do |f| %>
  <% if @level2_topic_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@level2_topic_group.errors.count, "error") %> prohibited this level2_topic_group from being saved:</h2>

      <ul>
      <% @level2_topic_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :start_year %><br>
    <%= f.date_select :start_year %>
  </div>
  <div class="field">
    <%= f.label :end_year %><br>
    <%= f.date_select :end_year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
