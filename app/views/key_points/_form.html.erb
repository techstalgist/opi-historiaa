<%= form_for(@key_point) do |f| %>
  <% if @key_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@key_point.errors.count, "error") %> prohibited this key_point from being saved:</h2>

      <ul>
      <% @key_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :topic_id %><br>
    <%= f.text_field :topic_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
